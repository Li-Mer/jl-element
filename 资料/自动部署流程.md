# GitHub Actions 自动部署流程详解

## 📚 什么是 GitHub Actions？

GitHub Actions 是 GitHub 提供的**持续集成/持续部署（CI/CD）**服务，可以在代码仓库中自动执行各种任务。

简单来说：

- 你推送代码 → GitHub Actions 自动运行 → 自动构建、测试、部署
- 无需手动操作，全程自动化
- 完全免费（公开仓库）

## 🎯 `deploy-docs.yml` 文件是干什么的？

这个文件是一个 **GitHub Actions 工作流配置文件**，它的作用是：

**当你推送代码到 GitHub 时，自动完成以下操作：**

1. ✅ 检出代码
2. ✅ 安装 Node.js 环境
3. ✅ 安装项目依赖
4. ✅ 构建 VitePress 文档
5. ✅ 部署到 GitHub Pages
6. ✅ 网站自动更新

**好处：**

- 🚀 不用手动构建和部署
- 🎯 每次更新文档自动发布
- 💪 减少人为错误
- ⚡ 节省时间

## 📝 文件详细讲解

让我们逐行解释 `deploy-docs.yml` 文件：

### 1. 基本信息

```yaml
# 部署 VitePress 文档到 GitHub Pages
name: Deploy Docs
```

- `name`：工作流的名称，会显示在 GitHub Actions 页面
- 这个名字可以自定义，如 "Deploy Documentation"

---

### 2. 触发条件

```yaml
on:
  # 当推送到 main 分支时触发
  push:
    branches:
      - main

  # 允许手动触发
  workflow_dispatch:
```

**`on`** 定义了什么时候运行这个工作流：

- **`push: branches: - main`**
  - 当有代码推送到 `main` 分支时自动触发
  - 例如：你运行 `git push origin main` 后，自动开始部署
- **`workflow_dispatch`**
  - 允许在 GitHub 网页手动触发
  - 在 Actions 页面点击 "Run workflow" 按钮即可

**示例场景：**

```bash
# 你修改了文档
git add docs/components/button.md
git commit -m "更新按钮文档"
git push origin main
# ⬆️ 推送后，工作流自动开始运行
```

---

### 3. 权限设置

```yaml
# 设置 GITHUB_TOKEN 的权限
permissions:
  contents: read # 读取仓库内容
  pages: write # 写入 GitHub Pages
  id-token: write # 写入 ID token（用于部署）
```

**为什么需要权限？**

- GitHub Actions 需要权限才能读取代码和部署网站
- `contents: read` - 允许读取你的代码
- `pages: write` - 允许更新 GitHub Pages 网站
- `id-token: write` - 用于安全认证

⚠️ **重要**：如果没有这些权限，部署会失败（404 错误）

---

### 4. 并发控制

```yaml
# 只允许一个并发部署
concurrency:
  group: pages
  cancel-in-progress: false
```

**作用：**

- 防止多个部署同时进行，避免冲突
- `cancel-in-progress: false` - 不取消正在运行的部署

**场景举例：**

- 你快速推送了两次代码
- 第二次部署会等待第一次完成
- 不会同时运行，避免混乱

---

### 5. 构建任务（Job: build）

```yaml
jobs:
  # 构建任务
  build:
    runs-on: ubuntu-latest # 在 Ubuntu 系统上运行
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # 获取完整的 Git 历史记录

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: 安装依赖
        run: npm ci

      - name: 构建 VitePress 文档
        run: npm run docs:build

      - name: 上传构建产物
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/.vitepress/dist
```

#### 步骤详解：

**Step 1: 检出代码**

```yaml
- name: 检出代码
  uses: actions/checkout@v4
  with:
    fetch-depth: 0
```

- 从 GitHub 下载你的代码到运行环境
- `fetch-depth: 0` - 获取完整历史记录（VitePress 需要用于显示"最后更新时间"）

**Step 2: 设置 Node.js**

```yaml
- name: 设置 Node.js
  uses: actions/setup-node@v4
  with:
    node-version: 20
    cache: npm
```

- 安装 Node.js 20 版本
- `cache: npm` - 缓存 npm 依赖，加快后续构建速度

**Step 3: 安装依赖**

```yaml
- name: 安装依赖
  run: npm ci
```

- `npm ci` - 安装 `package.json` 中的所有依赖
- 类似 `npm install`，但更快更稳定（适合 CI 环境）

**Step 4: 构建文档**

```yaml
- name: 构建 VitePress 文档
  run: npm run docs:build
```

- 运行 `npm run docs:build` 命令
- 这个命令在 `package.json` 中定义：`"docs:build": "vitepress build docs"`
- 生成静态 HTML 文件到 `docs/.vitepress/dist` 目录

**Step 5: 上传构建产物**

```yaml
- name: 上传构建产物
  uses: actions/upload-pages-artifact@v3
  with:
    path: docs/.vitepress/dist
```

- 将构建好的静态文件上传为 artifact（工件）
- 供下一个任务（deploy）使用

---

### 6. 部署任务（Job: deploy）

```yaml
# 部署任务
deploy:
  environment:
    name: github-pages
    url: ${{ steps.deployment.outputs.page_url }}
  needs: build # 依赖 build 任务完成
  runs-on: ubuntu-latest
  steps:
    - name: 部署到 GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
```

#### 步骤详解：

**任务配置**

```yaml
environment:
  name: github-pages
  url: ${{ steps.deployment.outputs.page_url }}
needs: build
```

- `needs: build` - 必须等待 build 任务完成
- `environment: github-pages` - 指定部署环境
- `url` - 部署完成后会显示网站地址

**部署步骤**

```yaml
- name: 部署到 GitHub Pages
  id: deployment
  uses: actions/deploy-pages@v4
```

- 将上一步上传的文件部署到 GitHub Pages
- 部署完成后，你的网站就更新了！

---

## 🔄 CI/CD 流程是什么？

### CI/CD 概念

**CI（Continuous Integration）- 持续集成**

- 开发者频繁地将代码合并到主分支
- 每次合并都自动运行测试
- 快速发现和修复问题

**CD（Continuous Deployment）- 持续部署**

- 代码通过测试后自动部署到生产环境
- 无需手动操作
- 快速交付新功能

### 完整的 CI/CD 流程示意图

```
┌─────────────────────────────────────────────────────────┐
│                    开发者操作                            │
└─────────────────────────────────────────────────────────┘
                          │
                          ├─ 修改代码
                          ├─ 提交代码: git commit
                          └─ 推送代码: git push
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                  GitHub 接收推送                         │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              触发 GitHub Actions 工作流                  │
│              (deploy-docs.yml)                          │
└─────────────────────────────────────────────────────────┘
                          │
        ┌─────────────────┴─────────────────┐
        ▼                                   ▼
┌───────────────┐                   ┌───────────────┐
│   CI 阶段     │                   │   CD 阶段     │
│  (持续集成)   │                   │  (持续部署)   │
└───────────────┘                   └───────────────┘
        │                                   │
        ├─ 1. 检出代码                      │
        ├─ 2. 安装环境                      │
        ├─ 3. 安装依赖                      │
        ├─ 4. 运行测试 ✓                   │
        └─ 5. 构建项目 ✓                   │
                          │                 │
                          └─────┬───────────┘
                                │
                                ├─ 6. 打包产物
                                ├─ 7. 上传到 GitHub Pages
                                └─ 8. 部署完成
                                │
                                ▼
                    ┌───────────────────────┐
                    │   用户访问新版本网站   │
                    │ https://你的域名      │
                    └───────────────────────┘
```

---

## 🎯 我们的项目中的 CI/CD 流程

### 传统手动部署 vs 自动化 CI/CD

#### ❌ 传统方式（手动）

```bash
# 每次更新文档都要手动执行：
npm run docs:build           # 1. 构建文档
cd docs/.vitepress/dist      # 2. 进入构建目录
git init                     # 3. 初始化 git
git add -A                   # 4. 添加文件
git commit -m 'deploy'       # 5. 提交
git push -f ...              # 6. 强制推送
```

**问题：**

- 😫 繁琐，容易出错
- ⏰ 浪费时间
- 🐛 可能忘记某个步骤
- 💻 需要在本地构建

---

#### ✅ 自动化方式（CI/CD）

```bash
# 只需要：
git add docs/components/button.md
git commit -m "更新按钮文档"
git push origin main

# GitHub Actions 自动完成剩下的所有工作！
```

**优势：**

- ⚡ 简单快速
- 🎯 不会出错
- 🚀 自动化部署
- ☁️ 在云端构建

---

## 🔍 完整执行流程实例

假设你要更新 Button 组件的文档：

### 第 1 步：修改文档

```bash
# 在 VSCode 中修改
docs/components/button.md
```

### 第 2 步：提交代码

```bash
git add docs/components/button.md
git commit -m "docs: 更新 Button 组件文档"
git push origin main
```

### 第 3 步：GitHub Actions 自动执行

**在 GitHub Actions 页面可以看到：**

```
⏳ Deploy Docs (运行中)
  ├─ ✓ build (23s)
  │   ├─ ✓ 检出代码 (2s)
  │   ├─ ✓ 设置 Node.js (3s)
  │   ├─ ✓ 安装依赖 (8s)
  │   ├─ ✓ 构建 VitePress 文档 (9s)
  │   └─ ✓ 上传构建产物 (1s)
  │
  └─ ✓ deploy (16s)
      └─ ✓ 部署到 GitHub Pages (16s)

✅ 部署成功！
   网站链接: https://li-mer.github.io/jl-element/
```

### 第 4 步：访问网站

```
打开浏览器：https://li-mer.github.io/jl-element/
看到更新后的按钮文档 ✓
```

**总耗时**：约 40 秒（自动完成）

---

## 💡 实际应用场景

### 场景 1：日常文档更新

```bash
# 你只需要：
1. 修改 markdown 文件
2. git push

# GitHub Actions 会自动：
✓ 构建 VitePress 站点
✓ 部署到 GitHub Pages
✓ 更新线上文档
```

### 场景 2：多人协作

```
开发者 A: 更新 Button 文档 → push
  └─ 自动部署 ✓

开发者 B: 更新 Dropdown 文档 → push
  └─ 自动部署 ✓（前一个完成后）

开发者 C: 修复文档错误 → push
  └─ 自动部署 ✓
```

### 场景 3：版本发布

```bash
# 发布新版本时：
git tag v1.0.0
git push origin main

# 自动触发：
✓ 构建文档（包含新版本信息）
✓ 部署到 GitHub Pages
✓ 用户看到最新版本文档
```

---

## 🔧 如何查看执行过程？

### 1. 进入 Actions 页面

```
GitHub 仓库 → 点击 "Actions" 标签
```

### 2. 查看工作流运行

- 🟡 黄色圆圈 = 正在运行
- ✅ 绿色对勾 = 成功
- ❌ 红色叉号 = 失败

### 3. 查看详细日志

点击任意工作流 → 点击任意步骤 → 查看完整输出

```
Run npm ci
npm WARN deprecated inflight@1.0.6
added 1000 packages in 8s
✓ 安装完成
```

---

## 🎓 关键概念总结

| 概念                   | 说明                 | 在我们项目中的体现      |
| ---------------------- | -------------------- | ----------------------- |
| **GitHub Actions**     | GitHub 的自动化平台  | 运行我们的部署工作流    |
| **Workflow（工作流）** | 自动化流程的配置文件 | `deploy-docs.yml`       |
| **Job（任务）**        | 工作流中的独立任务   | `build` 和 `deploy`     |
| **Step（步骤）**       | 任务中的具体操作     | 检出代码、安装依赖等    |
| **CI（持续集成）**     | 自动构建和测试       | 自动构建 VitePress      |
| **CD（持续部署）**     | 自动部署到生产环境   | 自动部署到 GitHub Pages |
| **Artifact（工件）**   | 构建产物             | 构建好的静态 HTML 文件  |

---

## 📐 文件结构说明

```
jl-element/
├── .github/
│   └── workflows/
│       └── deploy-docs.yml    ← 🎯 GitHub Actions 工作流配置
│
├── docs/
│   ├── .vitepress/
│   │   ├── config.ts          ← VitePress 配置
│   │   └── dist/              ← 构建输出目录（自动生成）
│   │       ├── index.html
│   │       ├── assets/
│   │       └── ...
│   └── components/
│       └── button.md
│
└── package.json               ← 包含 docs:build 脚本
```

**执行流程：**

```
1. GitHub Actions 读取 .github/workflows/deploy-docs.yml
2. 执行 npm run docs:build
3. 生成文件到 docs/.vitepress/dist
4. 上传并部署到 GitHub Pages
5. 网站更新完成
```

---

## 🚀 优化建议

### 1. 添加测试步骤

你可以在构建前添加测试：

```yaml
- name: 运行测试
  run: npm test
```

### 2. 添加通知

部署完成后发送通知：

```yaml
- name: 发送通知
  if: success()
  run: echo "部署成功！"
```

### 3. 缓存优化

GitHub Actions 已经缓存了 npm，构建速度会越来越快。

---

## 📚 扩展阅读

- **GitHub Actions 官方文档**: https://docs.github.com/en/actions
- **GitHub Pages 文档**: https://docs.github.com/en/pages
- **VitePress 部署指南**: https://vitepress.dev/guide/deploy
- **CI/CD 最佳实践**: https://www.atlassian.com/continuous-delivery

---

## 🎯 总结

**`deploy-docs.yml` 的本质：**

- 是一个自动化脚本
- 在 GitHub 云端运行
- 把"构建 + 部署"流程自动化

**核心价值：**

- ⚡ **省时**：从 5 分钟手动操作 → 0 秒（自动）
- 🎯 **准确**：避免人为错误
- 🚀 **高效**：推送代码即更新网站
- 💪 **专业**：符合现代软件工程实践

**记住这个公式：**

```
代码推送 → GitHub Actions → 自动构建 → 自动部署 → 网站更新
```

这就是现代化的 **CI/CD 流程**！🎉
